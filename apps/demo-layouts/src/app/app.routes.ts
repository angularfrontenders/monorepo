import { Route } from '@angular/router';

import { ArticleOneComponent } from '../components/articles/article_one.component';

export const appRoutes: Route[] = [{ path: 'article-one', component: ArticleOneComponent }];
